/**
 *
 *      ``IMPORTANT: Everything in Javascript happens inside an execution Context``.
 *
 * It has two components:
 * 1) Memory Component (Variables, key and values are stored, AKA known as VARIABLE COMPONENT).
 * 2) Code Component (Thread of Execution: Line by line execution)
 *
 *      ``IMPORTANT: javascript is a synchronous single-threaded language
 *                   (Execute one line at a time, once it is done executing, 
 *                   it will move on to next line)``
 *
 * */
